plugins {
    id 'java'
}

group 'com.github.jie65535.openchat'
version '0.3.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation fileTree(dir: 'lib', include: ['*.jar'])
}

jar {
    jar.baseName = 'openchat'

    destinationDir = file(".")
}